<%= form_for(@video) do |f| %>
  <% if @video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% @video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id %>

  <camera id='videohelpsubmission' data-app-id='a-6c6e5020-8b9f-0132-119e-22000a8c0328'></camera>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
